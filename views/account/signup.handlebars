<div class="login-container container">
 
  <div class="panel"  >
    <div class="panel-body">
      {{#if messages.error}}
        <div role="alert" class="alert alert-danger">
          {{#each messages.error}}
            <div>{{msg}}</div>
          {{/each}}
        </div>
      {{/if}}
      <form method="POST">
        <h1>Sign Up</h1>
        <div class="form-group">
           
          <input type="text" name="name" id="name" placeholder="Pick a username" class="form-control" autofocus>
        </div>
        <div class="form-group">
        
          <input type="email" name="email" id="email" placeholder="Email" class="form-control">
        </div>
        <div class="form-group">
      
          <input type="password" name="password" id="password" placeholder="Password" class="form-control">
        </div>
        <div class="form-group">
          <small class="text-muted">By signing up, you agree to the <a href="/">Terms of Service</a>.</small>
        </div>
        <button type="submit" class="btn btn-success">Sign up</button>
      </form>
      <div class="hr-title"><span>or</span></div>
      <div class="btn-toolbar text-center">
        <a href="/auth/google" class="btn btn-default">Sign in with Google</a>
        <a href="/auth/github" class="btn btn-default">Sign in with Github</a>
      </div>
    </div>
  </div>
  <p class="text-center">
    Already have an account? <a href="/login"><strong>Log in</strong></a>
  </p>
</div>
